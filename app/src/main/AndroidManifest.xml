<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/urlogo_with_background"
        android:label="habesha Recipe"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".finalp2"></activity>
        <activity android:name=".finalp" />
        <activity android:name=".yefisikDetailed" />
        <activity android:name=".yestomDetailed" />
        <activity android:name=".Yetsom" />
        <activity android:name=".Yefisik" />
        <activity android:name=".Home" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/splashTheme1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>